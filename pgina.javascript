constavanca=document.query selectorall(.btn-proximo)
avancafor each{
button add event listener('click',function(){
const atual= document. query selector ('ativo');
const proximo passo='passo'+ this.getiattribitte('data-proximo')
atual. classlist.remove ('ativo'):
document.getcelementbyid(proximo passo) classlist.add ('ativo')
})
})
